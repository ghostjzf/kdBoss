<!--index.wxml-->
<view class="container">
  <view wx:if="{{isHasStore}}">
    <view>负责门店</view>
  </view>
  <view wx:else>
    <view class='info'>*您还没有任何门店信息</view>
    <button class='btn-add' type='primary' bindtap='onAddStore'>新增门店</button>
  </view>
</view>
